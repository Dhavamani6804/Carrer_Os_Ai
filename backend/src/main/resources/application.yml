spring:
  application:
    name: CareerOS

  data:
    mongodb:
      uri: ${MONGODB_URI}
jwt:
   secret: ${JWT_SECRET}
   expiration: 86400000

server:
  port: 8080

gemini:
  api:
    key: ${GEMINI_API_KEY}
    model: gemini-3.5-flash
    temperature: 0.7
    max-output-tokens: 2048
